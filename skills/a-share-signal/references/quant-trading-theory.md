# 量化交易理论参考

仅当用户的分析体系依赖三周期共振、优化 KDJ、筹码分布、洗盘识别或评分模型的细节规则时，再读取这份参考。

## 目录

1. 三周期共振
2. 优化 KDJ
3. 量价资金共振
4. 筹码分布
5. 洗盘识别
6. 评分体系用法

## 1. 三周期共振

### 目标

判断日线、周线、月线的结构是否足够一致，从而支持更高置信度的方向判断。

### 核心规则

- 日线：短期触发与情绪
- 周线：中期趋势与资金行为
- 月线：长期趋势确认

如果只有一个周期转强，不要高估这个结构。

### 共振强度

当用户要求更明确的强度分级时，可以用这个加权思路：

```text
resonance_strength =
  0.3 * daily_strength +
  0.4 * weekly_strength +
  0.3 * monthly_strength
```

每个子分值控制在 `0-1`。

建议解释：
- `>= 0.85`：完全共振
- `>= 0.70`：强共振
- `>= 0.50`：部分共振
- `< 0.50`：共振不足

### 均线突破检查

如果体系依赖均线突破，每个周期优先检查这组均线：
- `5, 8, 13, 21, 34, 55`

建议使用突破比例：

```text
breakthrough_ratio = reclaimed_ma_count / total_ma_count
```

建议解释：
- `>= 0.90`：完全突破
- `0.70-0.89`：强突破
- `0.50-0.69`：弱突破
- `< 0.50`：未形成有效突破

更适合认定为有效共振的条件：
- 日线突破比例较强
- 周线突破比例较强
- 月线突破比例较强
- 成交量相对近期均量放大
- 资金流至少不拖后腿

## 2. 优化 KDJ

### 默认参数

当用户希望得到更稳定、假信号更少的中期信号时，优先使用 `KDJ(17,3,3)`，而不是 `KDJ(9,3,3)`。

### 公式

```text
RSV(n) = (close - lowest_n) / (highest_n - lowest_n) * 100
K_t = (2/3) * K_(t-1) + (1/3) * RSV_t
D_t = (2/3) * D_(t-1) + (1/3) * K_t
J_t = 3 * K_t - 2 * D_t
```

### 金叉分类

多头金叉：
- 前一根：`K <= D`
- 当前：`K > D`

强弱参考：
- 低位金叉：`K < 20` 且 `D < 20`，最强
- 中位金叉：`20 <= K <= 80`
- 高位金叉：`K > 80` 且 `D > 80`，最弱

### 多周期 KDJ 的用法

当体系要求同时看日线、周线、月线 KDJ 时：
- 看三个周期是否都处于金叉或多头维持状态
- 说明是不是日线先行、周线月线跟随确认
- 如果月线已经明显过热，要提醒信号偏晚

不要把一个高位钝化、明显过热的金叉，说成和刚启动的低位金叉同等质量。

## 3. 量价资金共振

当问题聚焦在触发质量时，使用这一部分。

### 最低确认条件

更高质量的多头触发，至少应同时满足：
- 价格突破关键均线或结构压力
- 成交量达到近期均量的 `1.2x`
- 资金流为正，或至少没有明显背离

建议强度分级：
- `1.2x`：基础确认
- `1.5x`：强确认
- `2.0x`：很强确认

### 解读规则

- 放量上涨：偏正面
- 放量下跌：抛压偏重
- 缩量上涨：持续性偏弱
- 缩量下跌：先等需求恢复

如果价格突破后很快跌回原区间或主筹码带，应判定为触发失败。

## 4. 筹码分布

如果用户体系高度依赖筹码分析，应与 [chip-framework.md](chip-framework.md) 结合使用。

### 优选结构

- 低位单峰密集
- 成本带集中
- 平均成本和主筹码区持续上移
- 当前价格位于主筹码区上沿附近或其上方

### 偏弱结构

- 上方多峰套牢盘
- 大涨后形成高位单峰
- 平均成本持续下移
- 当前价格跌到主筹码区下方

### 触发逻辑

较优触发条件：
- 价格突破主筹码区上沿
- 成交量同步放大
- 换手率活跃
- 上方没有明显沉重套牢区

## 5. 洗盘识别

当用户问某次回调是不是“洗盘”而不是出货时，使用这一部分。

### 良性洗盘特征

- 回调发生在上升结构中，而不是长期下降趋势中
- 下跌过程可控，没有破坏核心支撑区
- 回调时缩量，或短暂放量后迅速缩下去
- 价格在成本带或支撑位附近企稳，并出现承接反弹
- 筹码结构没有明显下移

### 出货特征

- 反复出现放量阴线
- 支撑跌破后修复质量很差
- 反弹始终过不了前方压力
- 筹码中心或成本带持续下移
- 反弹时资金流仍然偏弱

只有看到抛压被吸收、结构重新恢复，才能把它定义为洗盘。

## 6. 评分体系用法

当用户使用打分体系时：

- 每个评分项都要映射到真实可观察的数据
- 每一项明确标成通过、不通过或暂缺
- 总分只基于已支持的数据计算
- 明确区分硬性筛选项和加分项

建议回答结构：

```text
结论
总分 / 可评分满分
筛选条件
共振与 KDJ
筹码与洗盘判断
威科夫 / 缠论辅助判断
交易计划、失效条件、后续转强条件
```

避免伪精确。如果某个评分项缺少必要数据，就标记为暂缺，不要靠猜测补齐。
